region: ${env:REGION}
company: ${env:COMPANY}
team: ${env:TEAM}
stage: ${env:STAGE}

# make domainName configurable via environment variable
# to fix potential name collision on S3 buckets
# used for deployment bucket and in the productPhotos service
domainName: ${env:DOMAIN_NAME, hello-retail.biz}
